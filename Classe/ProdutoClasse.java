package Classe;

	/* "class" é utilizada para definir uma classe, que é um dos blocos fundamentais da programação orientada a objetos. 
	 * Uma classe em Java é um modelo ou um "plano" para criar objetos. 
	 * Ela descreve os atributos (variáveis) e comportamentos (métodos) que os objetos dessa classe irão ter. */

public class ProdutoClasse {
	
	/* Uma variável é um local na memória onde você pode armazenar dados. 
	 * Cada variável tem um tipo de dado associado, que define que tipo de informação pode ser armazenada nela.
	 * Variáveis em Java precisam ser declaradas com um tipo antes de serem usadas. */

	
	/* Variáveis de instância (ou membros da classe): 
	 * São variáveis definidas em uma classe, mas fora de qualquer método, e são usadas para definir as características dos objetos dessa classe.
	 * Exemplo: */
	
	
	String nome;
	double preco;
	double desconto;

	
	/* É importante observar que em Java, os nomes das variáveis seguem algumas regras, como não poder começar com um número, 
	 * não usar palavras-chave reservadas da linguagem como nomes de variáveis */
	
}
